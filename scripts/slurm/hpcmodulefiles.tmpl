#%Module1.0#######################################################################
## hpc modulefile
##
proc ModulesHelp { } {

        puts stderr "\tAdds hpc modules to module environment"
}

module-whatis   "adds hpc modules"
module use --append {NAS_MOUNT_POINT}/opt/modulefiles
